# PortalSDK Community - bfhub.app

## Differences

TBD

## Development

Clone with the submodule: `git clone git@github.com:bfhub-app/PortalSDK-Enhanced.git --recurse-submodules`

If you did not cloned with the submodules parameter, don't worry, do a `git submodule update --init` and git will download the files that it needs.


> Created by Nano
